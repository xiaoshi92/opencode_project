# Agent Instructions

This repository is currently an empty workspace.

## Environment & Configuration
- **Global Config Location:** `~/.config/opencode/opencode.jsonc` (specifies schema only).
- **OpenCode Installation:** `~/.opencode/` (contains Bun-compiled binary and SDK).
- **Default Provider:** Uses the built-in OpenCode provider. No custom API keys or base URLs are configured globally or locally.

## Project Setup
- Create a project-level `opencode.json` or `opencode.jsonc` to configure custom models/providers if needed.
- Standard project manifests (e.g., `package.json`, `Cargo.toml`, `requirements.txt`) should be created in the root directory.
